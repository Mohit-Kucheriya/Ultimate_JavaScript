/*

1. map() -
map returns a "new array" containing the results of applying an operation on all original
array elements.

2. filter() -
filter returns a "new array" containing the array elements that passed a specified test condition.
OR
elements for which the condition is true will be included in anew array.

3. reduce() - 
reduce boils (reduces) all array elements down to one single value (e.g. adding all elements together)


*/